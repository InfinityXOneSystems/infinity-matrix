apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: autofix-devops-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: autofix-devops.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: autofix-devops-service
            port:
              number: 80
