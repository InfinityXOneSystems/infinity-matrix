"""Setup configuration for infinity-matrix package."""
from setuptools import setup

# This file exists to support editable installs (pip install -e .)
# All configuration is in pyproject.toml
setup()
